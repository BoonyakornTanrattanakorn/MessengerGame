extends LevelEventHandler

func on_level_loaded() -> void:
	# Reserved for tower-specific initialization after the level is loaded.
	pass

func handle_intro_for_level() -> void:
	# Tower currently has no intro cutscene.
	pass
